const Header = () => {
    const headerStyle={}

    return (
        <header className="header">
            <h1 style={headerStyle}>Fast React Pizza Co.</h1>
        </header>
    );
};

export default Header;